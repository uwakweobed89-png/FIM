# File Integrity Monitor Dependencies

# Core dependencies
pyyaml>=6.0.1      # Configuration file parsing
requests>=2.31.0   # SIEM integration (HTTP requests)

# Optional: For automatic .env loading
python-dotenv>=1.0.0

# Note: hashlib is built-in to Python (no install needed)
# Note: pathlib is built-in to Python (no install needed)
